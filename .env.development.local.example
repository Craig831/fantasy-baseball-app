# Copy to .env.development.local (gitignored) to override the default API URL
# in frontend/.env. Required when JellyBaseballV2 is bound to a non-default
# port locally (e.g. ASP.NET Core's https://localhost:7088 from launchSettings.json).
VITE_API_BASE_URL=http://localhost:5000
